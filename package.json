{
  "name": "letterpaths-workspace",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --filter editor dev",
    "build": "pnpm -r build",
    "check:lib": "pnpm --filter letterpaths build",
    "deploy": "pnpm --filter editor run deploy"
  }
}
