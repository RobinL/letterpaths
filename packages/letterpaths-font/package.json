{
  "name": "letterpaths-font",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Build installable fonts from letterpaths cursive geometry.",
  "scripts": {
    "export": "tsx src/export-geometry.ts",
    "build": "pnpm run export && uv run python src/build-font.py",
    "postbuild": "node ../../scripts/sync-font-asset.mjs",
    "verify": "uv run python src/verify-shaping.py",
    "all": "pnpm run build && pnpm run verify"
  },
  "dependencies": {
    "letterpaths": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "^5.3.0"
  }
}
