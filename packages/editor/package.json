{
  "name": "editor",
  "private": true,
  "version": "0.0.0",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "deploy": "pnpm build && gh-pages -d dist"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "gh-pages": "^6.3.0",
    "typescript": "^5.3.0",
    "vite": "^5.1.0"
  },
  "dependencies": {
    "letterpaths": "workspace:*"
  }
}
